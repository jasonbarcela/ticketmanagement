# Copy to .env and adjust for your MySQL server
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=code_and_locks
PORT=3001
JWT_SECRET=code_and_locks_secret_passphrase_key

# For large base64 photo uploads, set in MySQL server config (my.ini / my.cnf):
#   max_allowed_packet=16M
# The app also runs SET SESSION max_allowed_packet on each connection.
